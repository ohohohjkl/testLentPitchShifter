# testLentPitchShifter
executable.exe -shift (recommend <0.5) number_of_scaleData (ex: 100) .\\folderName\\ (ex: D:\home\data\)

the folder Data must contain two subfolders which are "realData" and "scaleData"
